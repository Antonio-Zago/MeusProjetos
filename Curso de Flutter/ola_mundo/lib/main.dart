import 'package:flutter/material.dart';
import 'app_widget.dart';

main() {
  runApp(AppWidget(
    nome: 'Antonio',
    idade: 22, //Passagem do parâmetro
  )); //Método do Flutter que recebe um widget
}
